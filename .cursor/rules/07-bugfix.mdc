---
description: Bug 修复流程 — 当用户报告 bug 或错误时应用
globs:
  - "**"
---

# Bug 修复流程

## 诊断步骤
1. **收集证据**：读取终端输出、浏览器控制台错误、截图
2. **定位范围**：确认是前端、后端还是数据层问题
3. **读取相关代码**：只读取与错误直接相关的文件（最小上下文）
4. **形成假设**：基于证据提出最可能的原因

## 修复步骤
1. 编写最小化修复（不要顺带重构无关代码）
2. 运行 linter 检查修改后的文件
3. 验证修复是否解决了原始问题
4. 检查是否引入了新问题

## 常见故障模式
| 症状 | 常见原因 |
|------|----------|
| `ENOTSUP` on listen | Windows 不支持 `reusePort` |
| `<a> nested in <a>` | wouter `<Link>` 内嵌套了 `<a>` 标签 |
| 401 on page load | 正常行为（未登录时 `/api/auth/me` 返回 401） |
| HMR 后状态异常 | 强制刷新页面（Ctrl+Shift+R） |
| 数据库 schema 不匹配 | 删除 data.db 后重启服务器 |
