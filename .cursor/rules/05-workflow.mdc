---
description: 循环式交付工作流 — 处理功能开发任务时应用
globs:
  - "**"
---

# 循环式交付工作流（Loop-Based Delivery）

执行任何非平凡功能开发时，必须遵循以下循环流程。

## 外层循环：需求 → 规格 → 交付

```
用户需求（自然语言）
  ↓
需求澄清（PRD：目标、用户故事、约束、验收标准）
  ↓
技术规格拆分（将一个需求拆为多个安全的工程增量）
  ↓
逐个规格执行内层循环
  ↓
交付
```

## 内层循环：Plan → Work → Review → Ship

### 1. Plan（计划）
- 列出需要修改/新建的文件
- 明确数据流：Schema → Storage → Route → Frontend
- 识别风险和依赖
- 产出：清晰的工作计划（使用 TodoWrite 工具记录）

### 2. Work（执行）
- 按计划逐步实现
- 后端优先：Schema → Storage → Route → 验证 API
- 前端跟进：页面组件 → 路由注册 → 验证 UI
- 每步完成后标记 TODO 进度

### 3. Review（审查）
- 检查 linter 错误（ReadLints）
- 确认 TypeScript 类型无误
- 验证 API 端点可正常调用
- 确认 UI 文案为中文
- 检查 data-testid 是否已添加

### 4. Ship（交付）
- 确认所有 TODO 完成
- 总结变更内容
- 如用户要求则提交 Git commit

## 失败处理
- Review 发现问题 → 回到 Work 修复 → 再次 Review
- 最多迭代 3 次，超过则暂停并向用户报告问题

## 上下文注入原则
- Plan 阶段：注入架构文档、现有模式、约束条件
- Work 阶段：注入具体文件内容、接口定义、已有实现模式
- Review 阶段：注入变更 diff、验收标准、已知故障模式
- 不要一次性读取所有文件 — 只读取当前步骤需要的最小上下文
